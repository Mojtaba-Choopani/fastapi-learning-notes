
# یادداشت‌های یادگیری FastAPI

## مقدمه

در توسعه نرم‌افزار، ساخت یک پروژه فقط به نوشتن منطق برنامه و قرار دادن آن روی یک سرور محدود نمی‌شود. در بسیاری از موارد، یک برنامه نیاز دارد با بخش‌های دیگر یک سیستم ارتباط برقرار کند؛ مانند یک اپلیکیشن موبایل، یک وب‌سایت، یک بات، یک داشبورد مدیریتی یا یک سرویس نرم‌افزاری دیگر.

در معماری پروژه API یک لایه ارتباطی بین سیستم‌های مختلف ایجاد می‌کند و به برنامه‌ها اجازه می‌دهد بدون وابستگی مستقیم به جزئیات داخلی پیاده‌سازی یکدیگر، داده‌ها را ارسال و دریافت کنند.

برای مثال، یک سرویس پیش‌بینی قیمت ارز دیجیتال را تصور کنید. این سرویس می‌تواند ورودی‌هایی مانند نام ارز دیجیتال و بازه زمانی را دریافت کند، داده‌ها را پردازش کند و قیمت پیش‌بینی‌شده را برگرداند. سپس همین قابلیت می‌تواند توسط یک اپلیکیشن موبایل، یک بات، یک داشبورد یا هر سیستم دیگری مورد استفاده قرار گیرد.

در اکوسیستم پایتون، **FastAPI** یک فریم‌ورک مدرن برای ساخت APIها و سرویس‌های بک‌اند است. این فریم‌ورک با تمرکز بر **کارایی، سادگی و استانداردهای مدرن وب** طراحی شده و به توسعه‌دهندگان کمک می‌کند APIهایی تمیز، مقیاس‌پذیر و قابل اعتماد ایجاد کنند.

در این یادداشت، مفاهیم اصلی FastAPI به‌صورت خلاصه و ساختاریافته بررسی می‌شوند؛ از موضوعات پایه مانند:

- پارامترهای مسیر (**Path Parameters**) و پارامترهای Query
- بدنه درخواست (**Request Body**) و مدل‌های Pydantic
- اعتبارسنجی داده‌ها و مدیریت خطاها
- مدل‌های پاسخ (**Response Models**)

و سپس ادامه پیدا می‌کند با موضوعات پیشرفته‌تر مانند:

- Dependency Injection
- OAuth2 و JWT
- Middleware و CORS
- اتصال به پایگاه داده
- APIRouter برای پروژه‌های بزرگ
- Streaming و Server-Sent Events
- Background Tasks
- تست و Debugging

این یادداشت تلاش می‌کند دید واضحی از FastAPI به شما بدهد.

## ۱. پارامترهای مسیر و کوئری (Path & Query Parameters)

در FastAPI مسیرهای API می‌توانند اطلاعات را از خود آدرس (Path) یا از پارامترهای اضافی درخواست (Query) دریافت کنند؛ FastAPI با استفاده از **Type Hint**ها مقدارها را بررسی (**Validation**) و مستندات API را به‌صورت خودکار تولید می‌کند.

- **Path Parameter** برای داده‌ای است که بخشی از مسیر URL محسوب می‌شود و معمولاً برای شناسایی یک منبع استفاده می‌شود؛ مثل `/items/5` که `5` مقدار `item_id` است.

- **Query Parameter** برای فیلتر، جستجو یا تنظیمات اختیاری درخواست استفاده می‌شود؛ مثل `/items?skip=10&limit=20`.

- با استفاده از **Type Hint**، FastAPI نوع داده را بررسی می‌کند و در صورت مقدار نامعتبر خطای مناسب برمی‌گرداند.

- با استفاده از **Enum** می‌توان مقدارهای مجاز یک پارامتر مسیر را محدود کرد و این محدودیت در مستندات **OpenAPI** نیز نمایش داده می‌شود.

- پارامترهایی که مقدار پیش‌فرض ندارند اجباری هستند و پارامترهایی که مقدار پیش‌فرض دارند (مثل `None` یا `0`) اختیاری محسوب می‌شوند.

- یک **Endpoint** می‌تواند همزمان چند **Path Parameter** و **Query Parameter** داشته باشد و بسته به نوع عملیات از متدهای مختلف HTTP استفاده کند:

| متد | کاربرد |
| --- | --- |
| GET | دریافت اطلاعات |
| POST | ایجاد یا ارسال اطلاعات |
| PUT | جایگزینی کامل یک منبع |
| PATCH | تغییر بخشی از یک منبع |
| DELETE | حذف اطلاعات |

## ۲. Pydantic و Request Body

برای دریافت داده‌های ارسالی در بدنه درخواست (**Request Body**) از مدل‌های **Pydantic** استفاده می‌شود؛ FastAPI به‌صورت خودکار JSON را به مدل تبدیل کرده، اعتبارسنجی (**Validation**) انجام می‌دهد و خطاهای مربوط به داده‌های نامعتبر را مدیریت می‌کند.

- برای تعریف **Request Body** یک کلاس از `BaseModel` می‌سازیم و آن را به‌عنوان پارامتر تابع دریافت می‌کنیم؛ FastAPI خودش JSON ورودی را **parse** و **validate** می‌کند.

- می‌توان همزمان **Body + Path + Query Parameter** داشت؛ FastAPI بر اساس نوع داده تشخیص می‌دهد هر پارامتر از کدام بخش درخواست دریافت شود.

- استفاده از `Annotated` روش مدرن برای افزودن متادیتا و قوانین اعتبارسنجی به پارامترها است؛ مثل:
  - `max_length` و `min_length` برای محدودیت طول
  - `pattern` برای بررسی فرمت متن
  - `alias` برای تغییر نام پارامتر
  - `deprecated` برای مشخص کردن قدیمی بودن
  - `title` و `description` برای مستندات بهتر

- با `include_in_schema=False` می‌توان یک پارامتر را از مستندات **OpenAPI** مخفی کرد.

- برای دریافت چند مقدار در Query می‌توان از لیست استفاده کرد؛ مثل:

  ```python
  q: Annotated[list[str], Query()] = ["foo", "bar"]
  ```

- با **Custom Validation** و ابزارهایی مثل `AfterValidator` در Pydantic می‌توان قوانین اعتبارسنجی سفارشی تعریف کرد؛ زمانی که محدودیت‌های آماده FastAPI کافی نباشند.
