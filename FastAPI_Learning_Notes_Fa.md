
# یادداشت‌های یادگیری FastAPI

## مقدمه

در توسعه نرم‌افزار، ساخت یک پروژه فقط به نوشتن منطق برنامه و قرار دادن آن روی یک سرور محدود نمی‌شود. در بسیاری از موارد، یک برنامه نیاز دارد با بخش‌های دیگر یک سیستم ارتباط برقرار کند؛ مانند یک اپلیکیشن موبایل، یک وب‌سایت، یک بات، یک داشبورد مدیریتی یا یک سرویس نرم‌افزاری دیگر.

در معماری پروژه API یک لایه ارتباطی بین سیستم‌های مختلف ایجاد می‌کند و به برنامه‌ها اجازه می‌دهد بدون وابستگی مستقیم به جزئیات داخلی پیاده‌سازی یکدیگر، داده‌ها را ارسال و دریافت کنند.

برای مثال، یک سرویس پیش‌بینی قیمت ارز دیجیتال را تصور کنید. این سرویس می‌تواند ورودی‌هایی مانند نام ارز دیجیتال و بازه زمانی را دریافت کند، داده‌ها را پردازش کند و قیمت پیش‌بینی‌شده را برگرداند. سپس همین قابلیت می‌تواند توسط یک اپلیکیشن موبایل، یک بات، یک داشبورد یا هر سیستم دیگری مورد استفاده قرار گیرد.

در اکوسیستم پایتون، **FastAPI** یک فریم‌ورک مدرن برای ساخت APIها و سرویس‌های بک‌اند است. این فریم‌ورک با تمرکز بر **کارایی، سادگی و استانداردهای مدرن وب** طراحی شده و به توسعه‌دهندگان کمک می‌کند APIهایی تمیز، مقیاس‌پذیر و قابل اعتماد ایجاد کنند.

در این یادداشت، مفاهیم اصلی FastAPI به‌صورت خلاصه و ساختاریافته بررسی می‌شوند؛ از موضوعات پایه مانند:

- پارامترهای مسیر (**Path Parameters**) و پارامترهای Query
- بدنه درخواست (**Request Body**) و مدل‌های Pydantic
- اعتبارسنجی داده‌ها و مدیریت خطاها
- مدل‌های پاسخ (**Response Models**)

و سپس ادامه پیدا می‌کند با موضوعات پیشرفته‌تر مانند:

- Dependency Injection
- OAuth2 و JWT
- Middleware و CORS
- اتصال به پایگاه داده
- APIRouter برای پروژه‌های بزرگ
- Streaming و Server-Sent Events
- Background Tasks
- تست و Debugging

این یادداشت تلاش می‌کند دید واضحی از FastAPI به شما بدهد.

## ۱. پارامترهای مسیر و کوئری (Path & Query Parameters)

در FastAPI مسیرهای API می‌توانند اطلاعات را از خود آدرس (Path) یا از پارامترهای اضافی درخواست (Query) دریافت کنند؛ FastAPI با استفاده از **Type Hint**ها مقدارها را بررسی (**Validation**) و مستندات API را به‌صورت خودکار تولید می‌کند.

- **Path Parameter** برای داده‌ای است که بخشی از مسیر URL محسوب می‌شود و معمولاً برای شناسایی یک منبع استفاده می‌شود؛ مثل `/items/5` که `5` مقدار `item_id` است.

- **Query Parameter** برای فیلتر، جستجو یا تنظیمات اختیاری درخواست استفاده می‌شود؛ مثل `/items?skip=10&limit=20`.

- با استفاده از **Type Hint**، FastAPI نوع داده را بررسی می‌کند و در صورت مقدار نامعتبر خطای مناسب برمی‌گرداند.

- با استفاده از **Enum** می‌توان مقدارهای مجاز یک پارامتر مسیر را محدود کرد و این محدودیت در مستندات **OpenAPI** نیز نمایش داده می‌شود.

- پارامترهایی که مقدار پیش‌فرض ندارند اجباری هستند و پارامترهایی که مقدار پیش‌فرض دارند (مثل `None` یا `0`) اختیاری محسوب می‌شوند.

- یک **Endpoint** می‌تواند همزمان چند **Path Parameter** و **Query Parameter** داشته باشد و بسته به نوع عملیات از متدهای مختلف HTTP استفاده کند:

| متد | کاربرد |
| --- | --- |
| GET | دریافت اطلاعات |
| POST | ایجاد یا ارسال اطلاعات |
| PUT | جایگزینی کامل یک منبع |
| PATCH | تغییر بخشی از یک منبع |
| DELETE | حذف اطلاعات |

## ۲. Pydantic و Request Body

برای دریافت داده‌های ارسالی در بدنه درخواست (**Request Body**) از مدل‌های **Pydantic** استفاده می‌شود؛ FastAPI به‌صورت خودکار JSON را به مدل تبدیل کرده، اعتبارسنجی (**Validation**) انجام می‌دهد و خطاهای مربوط به داده‌های نامعتبر را مدیریت می‌کند.

- برای تعریف **Request Body** یک کلاس از `BaseModel` می‌سازیم و آن را به‌عنوان پارامتر تابع دریافت می‌کنیم؛ FastAPI خودش JSON ورودی را **parse** و **validate** می‌کند.

- می‌توان همزمان **Body + Path + Query Parameter** داشت؛ FastAPI بر اساس نوع داده تشخیص می‌دهد هر پارامتر از کدام بخش درخواست دریافت شود.

- استفاده از `Annotated` روش مدرن برای افزودن متادیتا و قوانین اعتبارسنجی به پارامترها است؛ مثل:
  - `max_length` و `min_length` برای محدودیت طول
  - `pattern` برای بررسی فرمت متن
  - `alias` برای تغییر نام پارامتر
  - `deprecated` برای مشخص کردن قدیمی بودن
  - `title` و `description` برای مستندات بهتر

- با `include_in_schema=False` می‌توان یک پارامتر را از مستندات **OpenAPI** مخفی کرد.

- برای دریافت چند مقدار در Query می‌توان از لیست استفاده کرد؛ مثل:

  ```python
  q: Annotated[list[str], Query()] = ["foo", "bar"]
  ```

- با **Custom Validation** و ابزارهایی مثل `AfterValidator` در Pydantic می‌توان قوانین اعتبارسنجی سفارشی تعریف کرد؛ زمانی که محدودیت‌های آماده FastAPI کافی نباشند.

## ۳. اعتبارسنجی عددی و مدل‌های Query

علاوه بر بررسی نوع داده، می‌توان روی مقادیر عددی و پارامترهای **Query** قوانین دقیق اعتبارسنجی تعریف کرد؛ همچنین می‌توان چند **Query Parameter** مرتبط را در یک مدل **Pydantic** مدیریت کرد تا کد مرتب‌تر و قابل نگهداری‌تر باشد.

- برای محدود کردن مقدارهای عددی در **Path و Query Parameter** می‌توان از قوانین زیر استفاده کرد:

  - `gt` برای بزرگ‌تر از (`>`)
  - `ge` برای بزرگ‌تر یا مساوی (`≥`)
  - `lt` برای کوچک‌تر از (`<`)
  - `le` برای کوچک‌تر یا مساوی (`≤`)

- می‌توان چند **Query Parameter** را داخل یک **Pydantic Model** قرار داد و با `Query()` به FastAPI معرفی کرد؛ این کار باعث ساختاردهی بهتر کد می‌شود:

```python
params: Annotated[FilterParams, Query()]
```

- برای اعتبارسنجی هر فیلد داخل مدل می‌توان از `Field()` استفاده کرد:

```python
limit: int = Field(gt=0, le=100)
```

- با تنظیم:

```python
model_config = {"extra": "forbid"}
```

هر **Query Parameter** اضافی که در مدل تعریف نشده باشد، رد شده و باعث ایجاد خطا می‌شود.

## ۴. Body پیشرفته و مدل‌های تودرتو

مدل‌های **Request Body** فقط به داده‌های ساده محدود نیستند؛ می‌توان چند مدل را همزمان دریافت کرد، مدل‌های پیچیده و تو در تو ساخت و برای مستندات API مثال‌های دقیق تعریف کرد.

- می‌توان چند **Body Parameter** را همزمان در یک **Endpoint** دریافت کرد؛ در این حالت FastAPI هر مدل را به‌عنوان یک کلید جداگانه در JSON درخواست در نظر می‌گیرد.

- با `Body(embed=True)` می‌توان حتی یک مدل تکی را هم داخل یک کلید مشخص در JSON قرار داد.

- مدل‌های **Nested** امکان ساختاردهی داده‌های پیچیده را فراهم می‌کنند؛ یک مدل می‌تواند شامل مدل دیگری باشد:

```python
image: Image
```

یا شامل لیست و مجموعه‌ای از مدل‌ها باشد:

```python
images: list[Image]
tags: set[str]
```

- مدل‌ها می‌توانند چندین سطح تو در تو داشته باشند و FastAPI به‌صورت خودکار آن‌ها را **parse** و **validate** می‌کند.

- برای نمایش مثال‌ها در مستندات **OpenAPI** سه سطح اصلی وجود دارد:

  - `Field(examples=...)` برای مثال یک فیلد خاص
  - `model_config = {"json_schema_extra": ...}` برای مثال در سطح کل مدل
  - `Body(openapi_examples=...)` برای تعریف چند سناریوی مختلف در سطح **Endpoint** (مثل ورودی معتبر یا نامعتبر)
 
  ## ۵. Cookie و Header

علاوه بر **Path**، **Query** و **Body** می‌توان اطلاعات درخواست را از **Cookie** و **Header** هم دریافت کرد؛ این داده‌ها می‌توانند به‌صورت مستقیم یا با استفاده از مدل‌های **Pydantic** مدیریت شوند.

- دریافت **Cookie** و **Header** مشابه **Query Parameter** است، با این تفاوت که منبع داده از بخش **Cookie** یا **Header** درخواست HTTP است:

  - `Cookie()` برای خواندن مقدارهای ذخیره‌شده در Cookie
  - `Header()` برای دریافت اطلاعات ارسال‌شده در Header

- FastAPI به‌صورت خودکار مقدار Headerها و Cookieها را استخراج و اعتبارسنجی می‌کند.

- در `Header()` به‌صورت پیش‌فرض نام متغیر پایتون با تبدیل `_` به `-` به نام Header نگاشت می‌شود؛ با:

```python
Header(convert_underscores=False)
```

می‌توان این رفتار را غیرفعال کرد.

- می‌توان چند Header یا Cookie مرتبط را داخل یک **Pydantic Model** گروه‌بندی کرد تا ساختار کد منظم‌تر شود؛ مشابه روشی که برای **Query Model** استفاده می‌شود.

## ۶. Response Model و کدهای وضعیت

می‌توان شکل و محتوای پاسخ API را با **Response Model** کنترل کرد؛ این کار علاوه بر اعتبارسنجی خروجی، باعث جلوگیری از ارسال ناخواسته اطلاعات حساس و استاندارد شدن پاسخ‌ها می‌شود.

- با استفاده از `response_model` یا تعریف نوع خروجی تابع مثل:

```python
-> UserOut
```

می‌توان مشخص کرد چه داده‌هایی در پاسخ API قرار بگیرند؛ FastAPI خروجی را فیلتر و **validate** می‌کند.

- اگر مدل خروجی شامل نکرده باشد، فیلدهای حساس مثل `password` حتی در صورت وجود در داده برگشتی، از **Response** حذف می‌شوند.

- برای برگرداندن پاسخ‌های خاص و خارج از مدل معمول می‌توان مستقیماً از **Response**ها استفاده کرد:

  - `JSONResponse` برای ارسال JSON سفارشی
  - `RedirectResponse` برای هدایت کاربر به مسیر دیگر

- برای تعیین کد وضعیت HTTP می‌توان از:

```python
status_code=201
```

استفاده کرد؛ اما استفاده از ثابت‌های `status` خواناتر است:

```python
status_code=status.HTTP_201_CREATED
```

- تعریف صحیح **Response Model** باعث می‌شود قرارداد API مشخص‌تر باشد و مستندات **OpenAPI** نیز دقیق‌تر تولید شوند.

## ۷. فرم و آپلود فایل

علاوه بر دریافت داده به‌صورت **JSON**، می‌توان اطلاعات را از فرم‌های HTML و فایل‌های آپلودی نیز دریافت کرد؛ این قابلیت برای ساخت APIهایی که با مرورگر یا فایل‌ها کار می‌کنند کاربرد دارد.

- با `Form()` می‌توان داده‌هایی را از درخواست‌های نوع:

```text
application/x-www-form-urlencoded
```

دریافت کرد؛ مثل فرم‌های سنتی ورود (**Login**) در وب.

- برای دریافت فایل از دو روش اصلی استفاده می‌شود:

  - `File()` برای دریافت مستقیم محتوای فایل به‌صورت `bytes`؛ مناسب برای فایل‌های کوچک.

  - `UploadFile` برای فایل‌های بزرگ‌تر؛ چون از روش **streaming** استفاده می‌کند و مصرف حافظه کمتری دارد.

- می‌توان `File()` یا `UploadFile` را همراه با `Form()` استفاده کرد تا همزمان اطلاعات فرم و فایل را از یک درخواست دریافت کرد.

- برای استفاده از آپلود فایل باید پشتیبانی از `multipart/form-data` در درخواست وجود داشته باشد.

## ۸. مدیریت خطا

خطاها بخشی از طراحی API هستند؛ می‌توان خطاهای استاندارد ایجاد کرد یا با ساختن **Exception Handler**های سفارشی، نحوه نمایش و قالب پاسخ‌های خطا را کنترل کرد.

- با استفاده از:

```python
HTTPException(status_code=..., detail=...)
```

می‌توان به‌سادگی یک خطای HTTP با کد وضعیت و پیام مشخص برگرداند.

- `HTTPException` برای خطاهای معمول داخل **Endpoint**ها مثل پیدا نشدن یک منبع یا دسترسی غیرمجاز کاربرد دارد.

- می‌توان برای کنترل بیشتر، **Exception Handler سفارشی** تعریف کرد؛ برای مثال:

  - `StarletteHTTPException` برای مدیریت خطاهای HTTP
  - `RequestValidationError` برای مدیریت خطاهای مربوط به اعتبارسنجی داده‌های ورودی

- با **Handler**های سفارشی می‌توان فرمت پاسخ خطا را تغییر داد؛ مثلاً به‌جای JSON پیش‌فرض، پاسخ را به‌صورت متن ساده (**plain text**) یا ساختار دلخواه برگرداند.

## ۹. متادیتا، مستندسازی و بروزرسانی جزئی

در FastAPI علاوه بر ساخت **Endpoint**، می‌توان API را برای توسعه‌دهنده‌ها بهتر مستندسازی کرد و برای تغییرات جزئی داده‌ها مثل **PATCH**، فقط بخش‌هایی را که کاربر ارسال کرده به‌روزرسانی کرد.

- با استفاده از:

```python
tags=["items"]
```

می‌توان Endpointها را در **Swagger UI** دسته‌بندی کرد؛ همچنین **docstring** تابع به‌عنوان توضیحات (**Description**) آن Endpoint نمایش داده می‌شود.

- `jsonable_encoder` برای تبدیل داده‌های پیچیده مثل مدل‌های **Pydantic**، `datetime` و سایر آبجکت‌ها به ساختاری قابل تبدیل به **JSON** استفاده می‌شود.

- برای پیاده‌سازی بروزرسانی جزئی با متد **PATCH** بهتر است فقط فیلدهایی که کاربر ارسال کرده‌اند تغییر کنند:

```python
item.model_dump(exclude_unset=True)
```

فقط مقادیر ارسال‌شده را استخراج می‌کند.

- سپس با:

```python
item.model_copy(update=...)
```

یک نسخه جدید از مدل با تغییرات اعمال‌شده ساخته می‌شود.

- این الگو باعث می‌شود مقدارهای پیش‌فرض یا فیلدهایی که کاربر ارسال نکرده، ناخواسته جایگزین داده‌های قبلی نشوند.

## ۱۰. تزریق وابستگی (Dependency Injection)

با **Dependency Injection** می‌توان منطق‌های مشترک مثل احراز هویت، اتصال به دیتابیس یا پردازش پارامترها را یک‌بار تعریف کرد و در چند **Endpoint** دوباره استفاده کرد؛ بدون اینکه کد تکراری نوشته شود.

- یک تابع معمولی که ورودی می‌گیرد و مقداری برمی‌گرداند، با:

```python
Depends(func)
```

به **Endpoint** تزریق می‌شود.

- Dependencyها برای پیاده‌سازی منطق‌های مشترک مثل:

  - Pagination
  - Authentication
  - بررسی دسترسی‌ها
  - مدیریت Session دیتابیس

  استفاده می‌شوند.

- Dependency فقط محدود به تابع نیست؛ می‌تواند:

  - یک **کلاس** باشد
  - خودش Dependency دیگری داشته باشد (**Sub-dependency**)
  - در سطح یک **Router** یا کل برنامه به‌صورت **Global Dependency** تعریف شود

- با استفاده از `yield` می‌توان قبل و بعد از اجرای **Endpoint** کدی اجرا کرد؛ مثلاً:

  - قبل از درخواست: ساخت اتصال دیتابیس
  - بعد از پاسخ: بستن Session و آزاد کردن Resourceها

- این ساختار باعث جداسازی بهتر مسئولیت‌ها و قابل نگهداری‌تر شدن پروژه می‌شود.

## ۱۱. امنیت — OAuth2 + JWT

برای محافظت از APIها می‌توان از سیستم‌های احراز هویت مثل **OAuth2 و JWT** استفاده کرد؛ کاربر یک‌بار وارد می‌شود، توکن دریافت می‌کند و در درخواست‌های بعدی با ارسال آن، هویت او بررسی می‌شود.

- با:

```python
OAuth2PasswordBearer(tokenUrl="token")
```

FastAPI مشخص می‌کند که توکن از Header زیر دریافت می‌شود:

```text
Authorization: Bearer <token>
```

- رمز عبور کاربران نباید به‌صورت خام (**Plain Text**) ذخیره شود؛ با ابزارهایی مثل `pwdlib` و `PasswordHash` هش شده و هنگام ورود بررسی می‌شود.

- Endpoint مثل `/token` با استفاده از:

```python
OAuth2PasswordRequestForm
```

اطلاعات ورود را دریافت می‌کند و در صورت موفقیت یک **JWT Token** ایجاد می‌کند.

- JWT معمولاً شامل اطلاعاتی مثل زمان انقضا (`exp`) است تا اعتبار توکن محدود باشد.

- Dependencyهایی مثل:

  - `get_current_user` برای **decode** کردن توکن و پیدا کردن کاربر
  - `get_current_active_user` برای بررسی شرایط اضافی کاربر (مثل فعال بودن حساب)

  استفاده می‌شوند.

- یک روش ساده‌تر برای کنترل دسترسی، بررسی یک Header سفارشی مثل:

```text
X-Token
```

است؛ اما برای سیستم‌های احراز هویت واقعی معمولاً از **OAuth2 + JWT** استفاده می‌شود.

## ۱۲. Middleware و CORS

با استفاده از **Middleware** می‌توان منطق‌هایی را روی تمام درخواست‌ها و پاسخ‌ها اعمال کرد؛ همچنین با **CORS** مشخص می‌کنیم چه کلاینت‌هایی از دامنه‌های دیگر اجازه ارتباط با API را دارند.

- با:

```python
@app.middleware("http")
```

می‌توان یک **Middleware** عمومی ساخت که قبل و بعد از پردازش هر **Request** اجرا شود؛ برای مثال:

  - اندازه‌گیری زمان پردازش درخواست
  - افزودن Header به پاسخ
  - ثبت Log درخواست‌ها

- با:

```python
app.add_middleware(...)
```

می‌توان Middlewareهای آماده یا سفارشی مختلف را به برنامه اضافه کرد.

- ترتیب اجرای Middlewareها اهمیت دارد؛ زیرا درخواست‌ها و پاسخ‌ها از زنجیره Middlewareها عبور می‌کنند.

- `CORSMiddleware` برای کنترل دسترسی درخواست‌هایی استفاده می‌شود که از یک **Origin** متفاوت ارسال می‌شوند؛ مثلاً وقتی **Frontend** روی یک دامنه و **Backend** روی دامنه دیگری اجرا می‌شود.

- تنظیمات مهم CORS شامل:

  - `allow_origins` برای تعیین دامنه‌های مجاز
  - `allow_methods` برای تعیین متدهای HTTP مجاز
  - `allow_headers` برای تعیین Headerهای قابل قبول

- تنظیم درست **CORS** باعث می‌شود فقط کلاینت‌های مورد اعتماد بتوانند با API ارتباط برقرار کنند.

## ۱۳. دیتابیس رابطه‌ای (SQLModel)

برای کار با دیتابیس‌های رابطه‌ای می‌توان از **SQLModel** استفاده کرد که ترکیبی از مدل‌های **Pydantic** و **ORM** است؛ با جدا کردن مدل دیتابیس، ورودی و خروجی API، ساختار داده‌ها امن‌تر و قابل مدیریت‌تر می‌شود.

- یک مدل پایه مثل:

```python
HeroBase
```

می‌تواند فیلدهای مشترک را نگه دارد و مدل‌های مختلف از آن ارث‌بری کنند.

- مدل:

```python
Hero(HeroBase, table=True)
```

نشان‌دهنده جدول واقعی در دیتابیس است و **SQLModel** آن را به‌عنوان یک **Table** می‌شناسد.

- مدل‌های مخصوص API برای جدا کردن مسئولیت‌ها استفاده می‌شوند:

  - `HeroCreate` برای داده‌هایی که هنگام ساخت رکورد دریافت می‌شوند.
  - `HeroUpdate` برای تغییر اطلاعات موجود.
  - `HeroPublic` برای داده‌هایی که در پاسخ API نمایش داده می‌شوند.

- این جداسازی باعث می‌شود فیلدهای داخلی دیتابیس مثل:

```python
secret_name
```

به‌صورت ناخواسته در پاسخ API ارسال نشوند.

- این الگو همان مفهوم کنترل خروجی با `response_model` را دارد، اما در کنار **ORM** کمک می‌کند ساختار دیتابیس و API از هم جدا و تمیز باقی بمانند.
